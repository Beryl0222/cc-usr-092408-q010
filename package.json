{
  "name": "regional-food-brand-governance",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "地方小吃公用品牌穿透许可与整改治理（事件溯源）",
  "scripts": {
    "test": "node --test",
    "build": "for f in src/*.js; do node --check \"$f\" || exit 1; done"
  }
}
